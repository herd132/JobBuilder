@transactional 반드시 필요
DB갔다오는 테스트시 롤백이 되어야하므로 필요

BeforeAll, AfterAll
static 메서드에서만 사용 가능


assertnotEquals 예상값, 실제값

assertThrows 사용하려면 두번째 메서드에 반드시 람다표현식 사용해야함
예외가 발생할값을 넣어줘야함

1 + 1 = 2 x
1 + 1 = 3 o (예외발생)

예외가발생할클래스,
예외발생할것같은 코드


다른서비스에 의존성 주입
@Autowired