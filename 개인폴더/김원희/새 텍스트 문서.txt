빈문자열로 세팅하려는 경우 setInputValue()라는 함수가 필요함 (input 값 비우기)

setTodoList
빈문자열이 아니라 2가지가 들어있다고 가정(JS 객체)

화면에 렌더링하고 싶으면
return 하고 소괄호 열고나서부터 jsx문법을 이용(html 문법처럼 생김)

상태, 변수다룰 때 중괄호 부터 열고 사용함!
.map 배열을 다룰 수 있는 함수

todoList.map() 이부분 중요
소괄호 쳤을 때와 중괄호 쳤을 때 차이

외부의 css import 하려면 css 파일 명쓰면 됨
ex)    import "경로/index.css";

handleAddTodo() 
inputValue 가 빈문자열로 변함
-> 이를 확인하려면 input태그의 value에 세팅되어 있어야 함

map 안에 () => ()는 바로 return 시키는거고 () => {} return 시키지 않는 차이로 기억합니다.