<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jobbuilder.project.worker.model.mapper.WorkerMapper">

	<!--  로그인 ( 근로자 )  -->
	<select id="login" resultType="Worker">
		SELECT MEMBER_NO, MEMBER_EMAIL, MEMBER_PW, MEMBER_NAME, MEMBER_TEL, 
		TO_CHAR(ENROLL_DATE, 'YYYY"년" MM"월" DD"일" HH24"시" MI"분" SS"초"') ENROLL_DATE ,
		AUTHORITY, WORKER_NO, WORKER_ID, WORKER_ADDRESS, PROFILE_IMG,
		SIGN_UP_PATH, FAST_LOGIN_TOKEN, BLIND_LOGIN_ID, WORKER_MBTI		
		FROM "WORKER"
		JOIN "MEMBER" USING(MEMBER_NO) 
		WHERE WORKER_ID = #{workerId}
		AND MEMBER_DEL_FL = 'N'	
	</select>
	
	<!--  이메일 중복검사 ( 근로자 ) -->
	<select id="checkEmail" resultType="_int">
		SELECT COUNT(*) 
		FROM "WORKER"
		JOIN "MEMBER" USING "MEMBER_NO"
		WHERE MEMBER_DEL_FL ='N'
		AND MEMBER_EMAIL = #{memberEmail}
	</select>
	
	<!--  닉네임 중복검사  (탈퇴안한) -->
	<select id="checkNickname" resultType="_int">
	SELECT COUNT(*)
	FROM "WORKER"
	JOIN "MEMBER" USING "MEMBER_NO"
	WHERE MEMBER_DEL_FL = 'N' 
	AND  MEMBER_NICKNAME = #{workerNickname}
	</select>
	
	<!--  회원가입 ( 멤버 정보 ) -->
	<insert id="signupMember">	
		INSERT INTO "MEMBER"
		VALUES(SEQ_MEMBER_NO.NEXTVAL, 
			 #{memberEmail},
			 #{memberPw},
			 #{memberNickname},
			 #{memberTel},
			 #{memberAddress},			 
			 DEFAULT,
			 DEFAULT,
			 2	
			 );
			
	 </insert>
	 <!--  회원가입 ( 알바생 정보 )  -->
	 <insert id="signupWorker" useGeneratedKeys="true" parameterType="Worker">	
	 	<selectKey order="BEFORE" resultType="_int" keyProperty="memberNo">
			SELECT (SEQ_MEMBER_NO.NEXTVAL - 1 ) FROM DUAL
		</selectKey>
	 INSERT INTO "WORKER"
		VALUES(SEQ_WORKER_NO.NEXTVAL,
			#{workerId},
			#{workerNickname},
			#{workerAddress},
			#{prifileImg},
			#{signUpPath},
			#{fastLoginToken},
			#{blindLoginId},
			#{workerMbti},
			#{memberNo}
	 </insert>
	 
			 
			 
	
</mapper>
	