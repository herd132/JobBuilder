<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jobbuilder.project.worker.model.mapper.WorkerMapper">

	<!--  로그인 ( 근로자 )  -->
	<select id="login" resultType="Worker">
		SELECT MEMBER_NO, MEMBER_EMAIL, MEMBER_PW, MEMBER_NAME, MEMBER_TEL, 
		TO_CHAR(ENROLL_DATE, 'YYYY"년" MM"월" DD"일" HH24"시" MI"분" SS"초"') ENROLL_DATE ,
		AUTHORITY_NO, WORKER_NO, WORKER_ID, WORKER_NICKNAME, WORKER_ADDRESS, PROFILE_IMG,
		SIGN_UP_PATH, FAST_LOGIN_TOKEN, BLIND_LOGIN_ID, WORKER_MBTI,TO_CHAR(WORKER_BIRTHDATE, 'YYYY"년" MM"월" DD"일" HH24"시" MI"분" SS"초"') WORKER_BIRTHDATE
		FROM "WORKER"
		JOIN "MEMBER" USING(MEMBER_NO) 
		WHERE WORKER_ID = #{workerId}
		AND MEMBER_DEL_FL = 'N'	
	</select>
	
	<!--  이메일 중복검사 ( 근로자 ) -->
	<select id="checkEmail">
	SELECT COUNT(*) 
	FROM "MEMBER"	
	WHERE MEMBER_DEL_FL ='N'	
	AND MEMBER_EMAIL = #{memberEmail}
	</select>
	
	<!--  Id 중복검사 ( 근로자 )  -->
	<select id="checkId">
	SELECT COUNT(*)
	FROM "WORKER"
	JOIN "MEMBER" USING(MEMBER_NO)
	WHERE MEMBER_DEL_FL = 'N' 
	AND  WORKER_Id = #{workerId}
	</select>
	
	
	<!--  닉네임 중복검사  (탈퇴안한) -->
	<select id="checkNickname">
	SELECT COUNT(*)
	FROM "WORKER"
	JOIN "MEMBER" USING(MEMBER_NO)
	WHERE MEMBER_DEL_FL = 'N' 
	AND  WORKER_NICKNAME = #{workerNickname}
	</select>
	
	<!--  전화번호 중복검사 -->
	<select id="checkTel">
	SELECT COUNT(*)
	FROM "WORKER"
	JOIN "MEMBER" USING(MEMBER_NO)
	WHERE MEMBER_DEL_FL = 'N'
	AND MEMBER_TEL = #{memberTel}
	</select>
	
	
	<!--  회원가입 ( 멤버 정보 ) -->
	<insert id="signupMember">	
		INSERT INTO "MEMBER"
		VALUES(SEQ_MEMBER_NO.NEXTVAL, 
			 #{memberEmail},
			 #{memberPw},
			 #{memberName},
			 #{memberTel},			 			 
			 DEFAULT,
			 DEFAULT,
			 2
			 )
			
	 </insert>
	 <!--  회원가입 ( 알바생 정보 )  -->
	 <insert id="signupWorker" useGeneratedKeys="true" parameterType="Worker">	
	 	<selectKey order="BEFORE" resultType="_int" keyProperty="memberNo">
			SELECT (SEQ_MEMBER_NO.NEXTVAL - 1 ) FROM DUAL
		</selectKey>
	 INSERT INTO "WORKER"
		VALUES(SEQ_WORKER_NO.NEXTVAL,
			#{workerId},
			#{workerNickname},
			#{workerBirthDate},
			TO_DATE(#{workerAddress}, 'yyyy-MM-dd'),			
			DEFAULT,
			#{signUpPath},
			#{fastLoginToken},
			#{blindLoginId},
			#{workerMbti},
			#{memberNo})
	 </insert>
	 
	 <!--  이메일로 아이디 찾기  -->
	 <select id="workerFindEmail" resultType="Worker">
	 	SELECT * FROM "WORKER"
	 	JOIN "MEMBER" USING(MEMBER_NO)
	 	WHERE MEMBER_EMAIL = #{memberEmail}
	 	AND MEMBER_NAME = #{memberName}
	 	AND MEMBER_DEL_FL = 'N' 	
	 </select>
			 
	 <!--  비밀번호 찾기 -->
	 <select id="workerFindPw">	
	 SELECT MEMBER_NO, MEMBER_EMAIL, MEMBER_PW, MEMBER_NAME, WORKER_NICKNAME, 
		MEMBER_TEL, WORKER_ADDRESS, PROFILE_IMG, AUTHORITY_NO,
		TO_CHAR(ENROLL_DATE, 'YYYY"년" MM"월" DD"일" HH24"시" MI"분" SS"초"') ENROLL_DATE
	 FROM "WORKER"
	 JOIN "MEMBER" USING(MEMBER_NO)
	 WHERE MEMBER_NAME = #{memberName}
		AND MEMBER_TEL = #{memberTel}
		AND MEMBER_EMAIL = #{memberEmail}
		AND WORKER_ID = #{workerId}
		AND MEMBER_DEL_FL = 'N'
	 </select>
	 
	 <!--  비밀번호 찾기 후 비밀번호 변경 -->	 
	 <update id="findChangePw">
		UPDATE "MEMBER"
		SET MEMBER_PW = #{encPw}
		WHERE MEMBER_NO = ${memberNo}
	</update>
			 
</mapper>
	