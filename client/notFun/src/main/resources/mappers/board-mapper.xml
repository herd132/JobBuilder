<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jobbuilder.project.board.model.mapper.BoardMapper">

<resultMap type="Board" id="board_rm">
	<!-- id 태그 : PK 역할을 하는 컬럼, 필드를 작성하는 태그 -->
	<id property="boardNo" column="BOARD_NO"/>
	
	<!-- collection 태그 : SELECT로 조회된 결과를 컬렉션(List)에 
			담아 지정된 필드에 세팅
			
			property : List를 담을 DTO의 필드명
			select   : 실행할 select의 id
			column   : 조회 결과 중 지정된 컬럼의 값을 파라미터로 전달..
			javaType : List(컬렉션)의 타입을 지정..
			ofType   : List(컬렉션)의 제네릭(타입제한) 지정..
	 -->
	 <!--  해당 게시글 이미지 목록 조회 후 필드에 정렬 -->
	<collection
	property="imageList"
	select="selectImageList"
	column="BOARD_NO"
	javaType="java.util.ArrayList"
	ofType="BoardImg"/>
	
	<!--  해당 게시글 댓글 목록 조회 후 필드에 저장 -->
	<collection
	property="commentList"
	select="selectCommentList"
	column="BOARD_NO"
	javaType="java.util.ArrayList"
	ofType="Comment"/>
</resultMap>



<!--  게시판 ( 교류게시판 / 알바경험담 / 면접후기 )  -->
<select id="selectBoardTypeList">
SELECT BOARD_CODE "boardCode", BOARD_NAME "boardName"
FROM BOARD_TYPE
ORDER BY BOARD_CODE
</select>	

<!--  게시글 개수 조회 -->
<select id="getListCount">
SELECT COUNT(*) FROM "BOARD"
WHERE BOARD_DEL_FL = 'N'
AND BOARD_CODE = #{boardCode}
</select>

<!--  게시판의 지정된 페이지 목록 조회 -->
<select id="selectBoardList" >
SELECT BOARD_NO, BOARD_TITLE, MEMBER_NAME, BOARD_READ_COUNT , MEMBER_NAME, 
(SELECT COUNT(*) FROM "COMMENT_BOARD" C	WHERE C.BOARD_NO = B.BOARD_NO
AND COMMENT_DEL_FL_BOARD = 'N') COMMENT_COUNT, 
	<![CDATA[
    CASE 
	WHEN SYSDATE - BOARD_WRITE_DATE < 1 / 24 / 60 --초단위
	THEN FLOOR((SYSDATE - BOARD_WRITE_DATE) * 24 * 60 * 60) || '초 전' 
	
	WHEN SYSDATE - BOARD_WRITE_DATE < 1 / 24 -- 분단위
	THEN FLOOR((SYSDATE - BOARD_WRITE_DATE) * 24 * 60) || '분 전'
	
	WHEN SYSDATE - BOARD_WRITE_DATE < 1  -- 시간단위
	THEN FLOOR((SYSDATE - BOARD_WRITE_DATE) * 24) || '시간 전' 
	
	ELSE TO_CHAR(BOARD_WRITE_DATE, 'YYYY-MM-DD')
	END BOARD_WRITE_DATE
	]]>	
	FROM "BOARD" B
	JOIN "MEMBER" USING(MEMBER_NO)

	WHERE BOARD_DEL_FL = 'N'
	AND BOARD_CODE = #{boardCode}
	ORDER BY BOARD_NO DESC	
</select>

<!--  상세 조회한 게시글 -->
<select id="selectOne">
	SELECT BOARD_NO, BOARD_TITLE, BOARD_CONTENT, BOARD_CODE, BOARD_READ_COUNT,
	MEMBER_NO, MEMBER_NAME, 
	TO_CHAR(BOARD_WRITE_DATE, 'YYYY"년" MM"월" DD"일" HH24:MI:SS') BOARD_WRITE_DATE,
	TO_CHAR(BOARD_UPDATE_DATE, 'YYYY"년" MM"월" DD"일" HH24:MI:SS') BOARD_UPDATE_DATE
	FROM "BOARD"
	JOIN "MEMBER" USING(MEMBER_NO)
	WHERE BOARD_DEL_FL = 'N' 
	AND BOARD_CODE = #{boardCode}
	AND BOARD_NO = #{boardNo}	
</select>



<!--  조회수 1 증가 -->
<update id="updateReadCount">
	UPDATE "BOARD" SET
	BOARD_READ_COUNT = BOARD_READ_COUNT + 1
	WHERE BOARD_NO = #{boardNo}
</update>

<!--  조회수 조회 -->
<select id="selectReadCount">
	SELECT BOARD_READ_COUNT
	FROM "BOARD"
	WHERE BOARD_NO = #{boardNo}
</select>



</mapper>
