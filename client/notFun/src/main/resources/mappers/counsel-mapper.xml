<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jobbuilder.project.counsel.model.mapper.CounselMapper">
	
	<select id="loginCounselor">
	
		SELECT 

			MEMBER_NO, 
			MEMBER_EMAIL, 
			MEMBER_PW, 
			MEMBER_NAME, 
			MEMBER_TEL, 
			ENROLL_DATE, 
			AUTHORITY_NO
		
		FROM "MEMBER"
		WHERE MEMBER_EMAIL = #{memberEmail}
		AND AUTHORITY_NO = 4
	</select>
	
</mapper>
